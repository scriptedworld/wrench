{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://scriptedworld.github.io/wrench/envelope.schema.json",
  "title": "envelope",
  "description": "A result envelope. Every producer in the ecosystem writes one of these, and every consumer reads it the same way. success is the only key every envelope carries; the rest is open so whatever detail a producer holds can travel with the result.",

  "type": "object",
  "required": ["success"],

  "properties": {
    "version": {
      "type": "string",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-((?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\\.(?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\\+([0-9a-zA-Z-]+(?:\\.[0-9a-zA-Z-]+)*))?$",
      "description": "The version of this format the document claims to conform to, as semver. Optional, and absent means the document claims nothing, which is every document written before this field existed. Present, it lets a consumer refuse a major it does not understand rather than failing later on a field it cannot find."
    },

    "success": {
      "type": "boolean",
      "description": "Whether what this envelope describes passed. The authoritative verdict."
    },

    "reasons": {
      "type": "array",
      "description": "Why. Present when success is false. A member's shape is open beyond the two keys every consumer can count on.",
      "items": { "$ref": "#/$defs/reason" }
    },

    "metadata": {
      "type": "object",
      "description": "Optional. Carries statistics and evidence where a producer has them.",
      "properties": {
        "statistics": {
          "type": "object",
          "description": "Whatever the producer counted, keyed by what it counted."
        },
        "evidence": {
          "type": ["object", "array"],
          "description": "What the result rests on: a list of paths, or a mapping keyed by what each is evidence for.",
          "$comment": "Carrying a description and no type, this accepted a bare string and a number, and a producer writing bare strings validated for as long as nobody read the requirement row. Both collection shapes are permitted because both are in use and neither is wrong: a list says where to look, a mapping says what each path belongs to. Requiring the mapping alone was proposed and refused, because it invalidates the shape this repository's own canonical fixture declares. Constraining the keys or the member shape would bind every producer to one runner's naming, which is a larger claim than this schema is entitled to make."
        }
      }
    }
  },

  "if": {
    "properties": { "success": { "const": false } },
    "required": ["success"]
  },
  "then": {
    "required": ["reasons"],
    "properties": {
      "reasons": { "minItems": 1 }
    }
  },

  "$defs": {
    "reason": {
      "type": "object",
      "required": ["message", "kind"],
      "properties": {
        "message": {
          "type": "string",
          "description": "Renderable by any consumer, which is what makes it required."
        },
        "kind": {
          "type": "string",
          "minLength": 1,
          "description": "What sort of thing this is, so a consumer tells a missing tool from a tool that found problems without reading English. The vocabulary is deliberately not enumerated here: a closed list would make a schema change the price of a new kind of failure."
        }
      }
    }
  }
}
