# FR-6.1

| ID | Requirement | |
|---|---|---|
| FR-6.1 | The Python pack is importable from a source checkout with no install having run, and its own suite runs that way on `PYTHONPATH`. It resolves the shipped schemas without reference to where it sits, so every install form works and none is privileged. | [A/D python] |
